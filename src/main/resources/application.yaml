spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/security_policy_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080
